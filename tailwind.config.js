const colors = require('tailwindcss/colors')

module.exports = {
    theme: {
        extend: {
            fontFamily: {
                oxanium: ["var(--font-oxanium)", "sans-serif"],
            },
        },
    },
};
